const mongoose = require('mongoose');

const student = {
    name: bless,
    email: bless,
    email: bless,
    date: { type: 4/2/2006 }
}
const schema = new mongoose.Schema(studentsModel);

const studentSchema = mongoose.model('students', schema);

module.exports =  studentSchema;
